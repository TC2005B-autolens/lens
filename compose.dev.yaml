services:
  api:
    command: bun dev
    build:
      context: .
      target: prerelease
    develop:
        watch:
          - action: rebuild
            path: package.json
          - action: sync
            path: src
            target: /usr/src/app/src
    environment:
      - NODE_ENV=development